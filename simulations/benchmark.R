# To conduct the simulation, run the `sim-code/simulate.R` script.

# NOTE: This takes multiple weeks depending on the machine. The results are about ~11 GB and hence too big for GitHub. All results are uploaded in a Docker image and can be downloaded there.

if (FALSE) source("sim-code/simulate.R")
